# Copy to .env; shell variables take precedence.
# Compact extraction: --data-root > HEPML_DATA_ROOT > samples.yaml data_root.
#HEPML_DATA_ROOT=/path/to/new/server/production

# All data/model outputs use <root>/<study>/{compact,datasets,runs,releases}.
# Download complete exports into C:/path/to/data/cg_bbc/compact/.
#HEPML_OUTPUT_ROOT=C:/path/to/data
